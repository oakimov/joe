/* Built-in files
 *
 * REMOVED from the live link (see build.zig).
 * Path A JOE `builtins[]` now lives in `src/builtins_data.zig`.
 * Regenerate historically via stringify; keep this tombstone.
 */
