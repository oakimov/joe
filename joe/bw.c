/*
 *	Edit buffer window generation
 *	Copyright
 *		(C) 1992 Joseph H. Allen
 *
 *	This file is part of JOE (Joe's Own Editor)
 *
 *	REMOVED from the live link (see build.zig).
 *	Path A JOE `bw.h` ABI + paint/lifecycle/helpers now live in
 *	`src/bw_lgen.zig` (Zig exports `bwgen`/`bwmk`/`bwfllw`/… + globals).
 *	`joe/bw.h` remains the C declaration surface for remaining C callers.
 */
