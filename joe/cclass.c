/*
 *	Character classes
 *
 *	REMOVED from the live link (see build.zig).
 *	Path A JOE `cclass.h` ABI now lives in `src/cclass.zig`
 *	(Zig exports interval/rset/rtree/rmap/cclass helpers).
 *	`joe/cclass.h` remains the C declaration surface.
 */
