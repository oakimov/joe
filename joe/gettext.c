/*
 *	JOE's gettext() library
 *
 *	REMOVED from the live link (see build.zig).
 *	Path A JOE `gettext.h` ABI now lives in `src/gettext.zig`
 *	(Zig exports my_gettext/init_gettext).
 *	`joe/gettext.h` remains the C declaration surface for remaining C callers.
 */
