/*
 *	Position history
 *	Copyright
 *		(C) 1992 Joseph H. Allen
 *
 *	This file is part of JOE (Joe's Own Editor)
 *
 *	REMOVED from the live link (see build.zig).
 *	Path A JOE `poshist.h` ABI now lives in `src/poshist.zig`
 *	(Zig exports afterpos/aftermove/windie/uprevpos/unextpos).
 *	`joe/poshist.h` remains the C declaration surface for remaining C callers.
 */
