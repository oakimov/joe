/*
 *	SELinux helpers
 *
 *	REMOVED from the live link (see build.zig).
 *	Path A JOE `selinux.h` ABI now lives in `src/selinux.zig`
 *	(Zig exports copy/match/reset/output_security_context; WITH_SELINUX off → no-ops).
 *	`joe/selinux.h` remains the C declaration surface.
 */
