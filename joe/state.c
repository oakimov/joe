/*
 *	JOE state file
 *	Copyright
 *		(C) 1992 Joseph H. Allen
 *
 *	This file is part of JOE (Joe's Own Editor)
 *
 *	REMOVED from the live link (see build.zig).
 *	Path A JOE `state.h` ABI now lives in `src/state.zig`
 *	(Zig exports joe_state/load_state/save_state).
 *	`joe/state.h` remains the C declaration surface for remaining C callers.
 */
