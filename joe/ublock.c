/*
 * 	Highlighted block functions
 *	Copyright
 *		(C) 1992 Joseph H. Allen
 *
 *	This file is part of JOE (Joe's Own Editor)
 *
 *	REMOVED from the live link (see build.zig).
 *	Path A JOE `ublock.h` ABI now lives in `src/ublock.zig`
 *	(Zig exports marks/rect/indent/filter/case/blksum + globals).
 *	`joe/ublock.h` remains the C declaration surface for remaining C callers.
 */
