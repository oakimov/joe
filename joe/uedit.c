/*
 *	Basic user edit functions
 *	Copyright
 *		(C) 1992 Joseph H. Allen
 *
 *	This file is part of JOE (Joe's Own Editor)
 *
 *	REMOVED from the live link (see build.zig).
 *	Path A JOE `uedit.h` ABI now lives in `src/uedit.zig`
 *	(Zig exports `pgamnt`/`u_goto_*`/`utomatch`/`utypebw`/`uquote`/…).
 *	`joe/uedit.h` remains the C declaration surface for remaining C callers.
 */
