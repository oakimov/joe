/*
 * 	User text formatting functions
 *	Copyright
 *		(C) 1992 Joseph H. Allen
 *
 *	This file is part of JOE (Joe's Own Editor)
 *
 *	REMOVED from the live link (see build.zig).
 *	Path A JOE `uformat.h` ABI now lives in `src/uformat.zig`
 *	(Zig exports center/paragraph/wrap/format/trim + `within`).
 *	`joe/uformat.h` remains the C declaration surface for remaining C callers.
 */
