/*
 * 	Shell-window functions
 *	Copyright (C) 1992 Joseph H. Allen
 *
 *	This file is part of JOE (Joe's Own Editor)
 *
 *	REMOVED from the live link (see build.zig).
 *	Path A JOE `ushell.h` ABI now lives in `src/ushell.zig`
 *	(Zig exports bknd/run/build/grep/kill + cstart/vt_scrdn/hists).
 *	`joe/ushell.h` remains the C declaration surface for remaining C callers.
 */
