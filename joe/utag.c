/*
 *	tags file symbol lookup
 *	Copyright
 *		(C) 1992 Joseph H. Allen
 *
 *	This file is part of JOE (Joe's Own Editor)
 *
 *	REMOVED from the live link (see build.zig).
 *	Path A JOE `utag.h` ABI now lives in `src/utag.zig`
 *	(Zig exports notagsmenu/utag/utagjump + tag_array).
 *	`joe/utag.h` remains the C declaration surface for remaining C callers.
 */
