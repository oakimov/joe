/*
 *	Terminal emulator
 *
 *	REMOVED from the live link (see build.zig).
 *	Path A JOE `vt.h` ABI now lives in `src/vt.zig`
 *	(Zig exports mkvt/vtrm/vt_data/vt_resize).
 *	`joe/vt.h` remains the C declaration surface.
 */
